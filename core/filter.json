{
	"description": "Runs the whole Bedrock Core filter stack — manifest, generator, guides, i18n, ui-compile, bundler — in the order they depend on.",
	"filters": [
		{
			"name": "Bedrock Core Stack",
			"runWith": "nodejs",
			"script": "./main.ts"
		}
	]
}
