{
  "name": "@bedrock-core/regolith-filters_manifest",
  "version": "2.0.0",
  "description": "Regolith filter that selects a manifest variant per profile and resolves its extends chain.",
  "type": "module",
  "engines": {
    "node": ">=22.18.0"
  },
  "scripts": {
    "test": "node --test \"test/**/*.test.ts\""
  }
}
