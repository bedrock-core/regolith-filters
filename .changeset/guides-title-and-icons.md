---
'@bedrock-core/regolith-filters_guides': minor
---

A page's `title` names it for the sidebar row and the page header only; the body renders exactly as the MDX is written, a leading `# heading` included. The 80-character warning on `icon` is gone.
