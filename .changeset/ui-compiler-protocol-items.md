---
'@bedrock-core/regolith-filters_ui-compiler': minor
---

Writes the container screen protocol items into the behaviour pack: one set per namespace of an entity or block a compiled screen opens from, under `BP/items/core-ui/<namespace>/`. A library that still places vanilla items has none to write.
