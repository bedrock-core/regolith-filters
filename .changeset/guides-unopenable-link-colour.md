---
'@bedrock-core/regolith-filters_guides': minor
---

A link nothing can open — a web address, or an anchor on the same page — is dark aqua (`§3`) rather than the blue (`§9`) of a link that opens a page, so a reader can tell the two apart.
