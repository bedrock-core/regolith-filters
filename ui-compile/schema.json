{
	"$schema": "http://json-schema.org/draft-07/schema#",
	"title": "ui-compile filter settings",
	"type": "object",
	"additionalProperties": false,
	"properties": {
		"namespace": {
			"type": "string",
			"pattern": "^[a-z0-9_]+$",
			"description": "The addon's namespace. It prefixes every screen's JSON UI namespace as <namespace>_<name>, so an addon's UI carries its own name and never collides with another addon's. Left unset, it is read from the creator/pack in core.register(), the way the other filters resolve it."
		}
	}
}
