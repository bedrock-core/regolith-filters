{
  "name": "@bedrock-core/regolith-filters_ui-compile",
  "version": "0.1.0",
  "description": "Regolith filter that compiles container screens written in JSX into static JSON UI.",
  "type": "module",
  "packageManager": "yarn@4.9.4",
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "esbuild": "^0.25.10"
  },
  "devDependencies": {
    "vitest": "^4.1.5"
  },
  "engines": {
    "node": ">=22.18.0"
  }
}
